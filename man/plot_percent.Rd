% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_percent}
\alias{plot_percent}
\title{Create percentage bar plots}
\usage{
plot_percent(
  df,
  group.by,
  variable,
  facet.by = NULL,
  cols = NULL,
  legend.ncol = 1,
  position = "stack",
  bar.width = 0.85,
  bar.color = "white",
  orientation = "vertical",
  y.label = "Frequency (\%)",
  x.label = "",
  theme = "line"
)
}
\arguments{
\item{df}{Dataframe containing the data to plot.}

\item{group.by}{Column name to group data by (x-axis).}

\item{variable}{Column name for variable to show as different colors (fill).}

\item{facet.by}{Optional column name to facet the plot by. Default is NULL.}

\item{cols}{Optional vector of colors for fill. Default is NULL (uses default ggplot colors).}

\item{legend.ncol}{Number of columns for the legend. Default is 1.}

\item{position}{Position adjustment for bars. Options are "stack" (default) or "fill".}

\item{bar.width}{Width of the bars. Default is 0.85.}

\item{bar.color}{Color of the bar borders. Default is "white".}

\item{orientation}{Plot orientation. Options are "vertical" (default) or "horizontal".}

\item{y.label}{Label for the y-axis. Default is "Frequency (\%)".}

\item{x.label}{Label for the x-axis. Default is "" (empty).}

\item{theme}{Theme to apply. Default is "line". Options are "line" or "border".}
}
\value{
A ggplot object
}
\description{
Generate bar plots showing percentages or frequencies across groups.
This function calculates percentages within groups and creates a stacked bar plot.
}
\examples{
\dontrun{
# Basic percentage plot
plot_percent(mtcars, group.by = "am", variable = "cyl")

# With custom colors
plot_percent(mtcars, 
             group.by = "am", 
             variable = "cyl", 
             cols = c("red", "blue", "green"))
             
# With faceting
plot_percent(mtcars, 
             group.by = "am", 
             variable = "cyl", 
             facet.by = "vs")
             
# Horizontal orientation
plot_percent(mtcars, 
             group.by = "am", 
             variable = "cyl", 
             orientation = "horizontal")
}
}
